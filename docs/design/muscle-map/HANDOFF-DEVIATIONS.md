# Muscle map — handoff deviation log

The handoff package under `momentum-muscle-map-handoff/` is the source of
truth and is never edited. This file records every place where the production
copy differs from it, why, and which stage made the change. Anything not
listed here is verbatim.

| # | Stage | File | Deviation | Why |
|---|---|---|---|---|
| 1 | 1 | `src/features/body/muscleMeshMap.ts` | The local `MuscleGroup` declaration is deleted and the type is imported from `src/core/model`. | The file's own comment asks for exactly this. The domain type is authoritative. |
| 2 | 1 | `src/features/body/tokens.ts` | The local `MuscleGroup` declaration is deleted and imported from `src/core/model`. `MuscleState` stays declared locally, verbatim; `tokens.test.ts` asserts at compile time that it equals the body renderer's `MuscleState` and at run time that `STATE_COLOR` and `BODY_REGIONS` cover the renderer's states and the domain's groups. | Same reason as 1. The five state names already exist in `src/components/BodyRenderer`, name for name, so no translation layer is needed and none is added. |
| 3 | 1 | `tools/validate-body-glb.mjs` | Copied unchanged. Wired as `npm run validate:body`, which runs first inside `npm run build` and therefore in the Pages workflow. | The handoff asks for the validator in CI. The repo had no `tools/` directory; one is created rather than renaming the script's documented path. |
| 4 | 1 | `vite.config.ts` (integration, not a handoff file) | The service-worker plugin no longer hardcodes `/Momentum-App/`; it reads the resolved Vite `base` in `configResolved` and precaches `${base}models/momentum-body.glb` alongside the icons. | The preview repository is built with `--base=/Momentum-preview/`, and a hardcoded production base would have precached the wrong URLs there (it already did for the shell). Verified: the production build lists `/Momentum-App/models/momentum-body.glb`, the preview build `/Momentum-preview/models/momentum-body.glb`, and neither contains the other's base. |
| 5 | 1 | `package.json` | `@types/three@0.160.0` added as a dev dependency. | `muscleMeshMap.ts` imports `Object3D` and `Mesh` types from `three`; the runtime package arrives in Stage 2, the types are needed for `tsc -b` now. Pinned exact, matching the handoff's three pin. |
| 6 | 1 | `THIRD-PARTY-NOTICES.md`, `docs/design/muscle-map/license.txt`, `docs/design/muscle-map/ATTRIBUTION.txt` | Added / copied verbatim. | ATTRIBUTION.txt's checklist. |
| 7 | 2 | `react/*` → `src/features/body/*` | The React sources cited `docs/body-model-contract.md`, which the handoff does not contain. Those comment references now point to `docs/design/muscle-map/momentum-muscle-map-handoff/HANDOFF.txt` (with the section). Comment-only. | The document is not part of the authoritative package and is not to be reconstructed. |
| 8 | 2 | `BodyViewer.tsx` | The view pills "Vorne / Seite / Hinten" and the group label "Ansicht" come from the i18n catalogue (`body.view`, `body.view.front/side/back`; en: View / Front / Side / Back). | Approved presentation-only change; the app is bilingual. |
| 9 | 2 | `useMuscleHighlight.ts` | The material treatment is the approved one: `regionMaterial()` (onBeforeCompile rim patch with `uRim`/`uRimColor` and own varyings), the retarget rules and the rim easing are ported from `body-viewer.js`; every constant is read from `tokens.ts` (`REGION_MATERIAL`, `BODY_BASE_COLOR`, `NEUTRAL_HIGHLIGHT`). The handoff's React file had the pre-approval mix 0.30/0.58, emissive 0/0.13, no rim and hardcoded colours. The epsilon snaps and the coalesced keepAlive are kept; the rim and rim colour ease with the same snaps. | HANDOFF §13 and the Stage 2 brief §2a. |
| 10 | 2 | `BodyModel.tsx` | `BODY_MODEL_URL` is `${import.meta.env.BASE_URL}models/momentum-body.glb` instead of the absolute `/models/momentum-body.glb`. The inert `none` primitive gets its own untinted material (roughness 0.8) as in `body-viewer.js`, created with the scene and disposed with it. A load error from `useGLTF` is forwarded to `onError`. | Base-safe under `/Momentum-App/` and `/Momentum-preview/`; brief §2b and §2a. |
| 11 | 2 | `useGLTF.ts` | Returns `{ gltf, error }` instead of swallowing a rejected load; the cache still forgets a rejection so the next mount retries. `BodyViewer` renders its `fallback` on a load error, not only on a contract violation. | Brief §2c: a failed fetch left a blank canvas. |
| 12 | 2 | `CameraRig.tsx` | Reads `prefers-reduced-motion` (once, then follows changes): no inertia coast on pointer up and a preset settles on the next frame; drag, tap and shortcuts unchanged. Adds the framing rule `fov = clamp(27 · 400 / heightPx, 22, 40)` from `body-viewer.js #resize`, applied whenever the R3F size changes. | Brief §2d and §3. |
| 13 | 2 | `BodyViewer.tsx` | No `height` prop and no fixed 380 px. The shell is host-sized: `width: 100%`, `aspect-ratio: 1 / 0.97`, `max-height: 400px` (bodyViewer.css), and the rig adapts the fov to the resulting height. Verified at 320/393/430 with no clipped head or feet through front, side, back and 230°. | Stage 1 clarification C: no fixed viewport. |
| 14 | 2 | `BodyViewer.tsx` | The WebGL capability probe is memoised per page and releases its own context (`WEBGL_lose_context`). | StrictMode calls a `useState` initialiser twice; each probe held an unused context that counted toward the browser's limit. Observed in `body.mjs` before the change. |
| 15 | 2 | `BodyViewer.tsx` | The RoomEnvironment PMREM texture is kept in a ref and disposed on unmount. | R3F disposes the renderer, not a texture the host attached to `scene.environment`. |
| 16 | 2 | `BodyViewer.tsx` | New optional prop `onRotate(angle, view)` — the React form of the `body-rotate` event (throttled while moving, once on the settled frame). `data-view` is set on the shell beside `data-azimuth`. | Brief §6 asks for a rotation-angle callback; the handoff's React file only reported the named view. |
| 17 | 2 | `bodyViewer.css` | The pill's colours use the design tokens (`--surface-input`, `--surface-elevated`, `--text-primary`, `--radius-pill`, `--type-caption`, weights) instead of the handoff's hex values; a `::before` inset gives the 30 px pill a 44 px hit area. Sizes, spacing and the 30 px pill are unchanged. | Momentum's interface rules: tokens only, 44 px targets. |
| 18 | 2 | `index.ts` (new) | A lazy boundary: `BodyViewer = lazy(() => import('./BodyViewer'))`, re-exporting the types, regions and tokens. Hosts import from here. | The chunk boundary the product requires; established now, wired in Stage 4. |
| 19 | 2 | `BodyDevPage.tsx` (new), `main.tsx` | A dev-only harness at `#/dev/body`, mounted instead of the app only when `import.meta.env.DEV` holds; not in the production bundle. Fixture states only. | Brief §10. |
| 21 | 4 | `useMuscleHighlight.ts` | **A selected region with no data now shows the neutral selection cue.** `stateIntensity()` returns 0 for `noData`, and the approved treatment multiplies colour, emissive and rim by it, so a successful tap on an untrained muscle looked exactly like a miss. The region keeps its base grey and its `noData` meaning; it takes the existing neutral branch (`NEUTRAL_HIGHLIGHT`, emissive 0.1, rim at 0.6 of selected) instead, which the handoff already defines for the tint-off case. No state colour is invented and no intensity is raised. The decision is now a pure function, `regionTargets()`, with `highlight.test.ts` pinning it. | Approved in Stage 4b, decision 2: interaction feedback. |
| 22 | 4 | `MuscleModule.tsx` | **A tap on the body selects and never clears.** The screen's row handler still toggles; the body and the flat figure get a select-only handler. | Approved in Stage 4b, decision 3: a second tap on the same region used to undo the first. |
| 23 | 4 | `MuscleRows.tsx` | The row carries the group's identity dot, its name, when it was last trained, the mini chart, and **either** the percentage **or** the state in words — not both. The approved design shows both; at Momentum's card width the long German states ("Noch nicht trainiert") leave the name about 20px. The number carries direction by its sign, so colour is never the only carrier. | The brief's own row spec, "current delta OR translated data state/status". |
| 24 | 4 | `MuscleRows.tsx` | The row's supporting line is the recency only, as in the handoff's own rows (`demo.js`, `sub: 'Zuletzt vor 2 Tagen'`). `latestExerciseLabel` is derived and on the view model but rendered nowhere: the Übungen list below already names the exercises of the selected group, and an id fallback must never reach the screen. Recency reads "Heute / Gestern / Vor N Tagen" rather than the design's longer "Zuletzt vor N Tagen", which wraps at this width. | HANDOFF §26 rows; Stage 4b §11–12. |
| 25 | 4 | `Sparkline.tsx` | The chart's geometry and scale are `spark80()` from the handoff, ported: points spread over 74px inside a 3px margin, the series' own min and max mapped into 16px, flat down the middle when the span is nil, a dot on the latest point, stroke 1.6 in the muted identity colour. **The scale is therefore per row and does not include zero** — the approved function's behaviour, kept in preference to the brief's zero-inclusive fallback, which applies only where the handoff defines nothing. | HANDOFF §26–27; Stage 4b §8 ("preserve that behaviour and say so"). |
| 26 | 4 | `bodyViewer.css` / `muscleModule.css` | The viewer's box is the host card's: full width, aspect 1:1.02, 280–360px. At ≤360px the rows wrap to two lines rather than truncating a name. | Stage 4a decision; Stage 4b §6. |
| 27 | 4 | `BodyViewer.tsx` | `frameloop` drops to `never` while the tab is hidden or the card is scrolled out of view, and returns to `demand` with one `invalidate()` on the way back. One listener and one observer, both released on unmount. | Stage 4b visibility hardening. |
| 28 | 4 | `BodyBoundary.tsx` (new) | The smallest local error boundary around the lazy viewer: a chunk that cannot be fetched and a throw after mount both fall back to the SVG figure. No global error infrastructure. | Stage 4b §14. |
| 20 | 2 | reference-only | `body-viewer.js` (source of the material port), `demo.js`, `index.html`, `styles.css`, `reference/*` are not copied into `src`. | Demo and reference material. |

## Not deviations

- `public/models/momentum-body.glb` is byte-identical to the handoff's
  (md5 `24fd71a7fc5249137eb6684b45336c35`).
- Every colour, number and function in `tokens.ts` is unchanged.
- The handoff's German `text` labels inside `STATE_COLOR` are kept as data
  but are **not** what the rows display; the rows keep the catalogue strings
  (`muscle.state.*`), which differ in one place ("Noch kein Vergleich").
